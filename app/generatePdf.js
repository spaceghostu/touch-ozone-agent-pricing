// import { jsPDF } from "jspdf";


export const generatePdf = (data) => {
    // const doc = new jsPDF('p', 'mm', [297, 210]);

    // console.log(doc);

    // let cursor = 20;


    // const sectionHeader = (text) => {

    //     doc.setFontSize(12);

    //     doc.setFillColor(191, 191, 191);
    //     // doc.setStrokeColor(0, 0, 0);
    //     doc.rect(20, cursor, 210, 10, 'DF');

    //     // cursor += 5;

    //     doc.text(cursor, 12, text);
    // }

    // // doc.text(JSON.stringify(data), 10, 10);

    // sectionHeader("TOUCH OZONE (PTY) LTD CUSTOMER REGISTRATION AND SALES ORDER PROCESS")

    // doc.save("a4.pdf");
}

// generatePdf({})